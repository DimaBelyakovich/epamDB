CREATE TABLE "seas_track_table" (
    "id" int NOT NULL,
    "season_id" int NOT NULL,
    "track_id" int NOT NULL,
    CONSTRAINT "pk_seas_track_table" PRIMARY KEY (
        "id"
     )
     ENABLE 
);